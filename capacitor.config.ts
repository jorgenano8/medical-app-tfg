import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'medical.app.tfg',
  appName: 'medical-app-tfg',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
